name: Flutter Build APK

on:
  push:
    branches: [ "main", "master" ]
  pull_request:
    branches: [ "main", "master" ]

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout Repository
        uses: actions/checkout@v4

      - name: Setup Java
        uses: actions/setup-java@v4
        with:
          distribution: 'zulu'
          java-version: '17'

      - name: Setup Flutter
        uses: subosito/flutter-action@v2
        with:
          # Versão estável que contém o Dart 3.5.0
          flutter-version: '3.24.0'
          channel: 'stable'
          cache: true

      - name: Install dependencies
        run: flutter pub get

      - name: Build APK Debug
        # Adicionei o --no-tree-shake-icons caso use ícones customizados que possam dar erro no build
        run: flutter build apk --debug --split-per-abi --no-tree-shake-icons -v

      - name: Upload APKs
        uses: actions/upload-artifact@v4
        with:
          name: bot-whatsapp-apks
          path: build/app/outputs/flutter-apk/*.apk
      - name: Upload APKs
        uses: actions/upload-artifact@v4
        with:
          name: bot-whatsapp-apks
          # O asterisco *.apk garante que ele pegue todos os arquivos gerados pelo split
          path: build/app/outputs/flutter-apk/*.apk
